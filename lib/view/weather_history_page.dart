import 'package:flutter/material.dart';

class WeatherHistory extends StatelessWidget {
  const WeatherHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
